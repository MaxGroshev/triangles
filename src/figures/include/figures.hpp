#pragma once

namespace geom_figures {

    const int count_of_dementions = 3;
    const double calculation_error = 0.02;

    struct point_t;
    struct line_t;
    struct vector_t;
    struct plane_t;
    class  triangle_t;
}
